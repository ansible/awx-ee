---
version: 1
build_arg_defaults:
  EE_BASE_IMAGE: 'daveshow/ansible-runner:devel'
  EE_BUILDER_IMAGE: 'daveshow/ansible-builder:devel'
dependencies:
  galaxy: _build/requirements.yml
  system: _build/bindep.txt
  python: _build/requirements.txt
additional_build_steps:
  append:
    - RUN alternatives --set python /usr/bin/python3
    - COPY --from=daveshow/receptor:devel /usr/bin/receptor /usr/bin/receptor
    - RUN mkdir -p /var/run/receptor
    - COPY --from=1password/op:2 /usr/local/bin/op /usr/bin/op
    - COPY --from=imega/jq:latest /usr/bin/jq /usr/bin/jq
    - COPY --from=imega/jq:latest /usr/lib/* /usr/lib/
    - COPY --from=imega/jq:latest /lib/* /lib/
    - COPY --from=hashicorp/packer:light /bin/packer /usr/sbin/packer
    - ADD run.sh /run.sh
    - CMD /run.sh
    - USER 1000
    - RUN git lfs install
